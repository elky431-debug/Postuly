import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Abonnement — Postuly",
  description: "Offre et facturation Postuly.",
};

export default function AbonnementLayout({ children }: { children: React.ReactNode }) {
  return children;
}
